serenity {
  project.name = "Proyecto Base Serenity BDD"
}

webdriver {
  driver = chrome
}

drivers {
  chrome {
    arguments = ["--start-maximized"]
  }
}

timeouts {
  implicit = 0
  fluentwait = 30
}
